# 验收清单

- [x] 插件清单文件正确描述 QuickDict，入口程序集正确
- [x] 示例演示工具栏项和设置视图已被移除
- [x] 词典服务抽象（`IWordEntry`、`IDictionaryService`）已存在
- [x] ECDICT 可加载并查询单词、音标、词性、释义、时态变化（已代码审查，待 ICC 宿主实测）
- [x] 用户可以通过设置面板导入本地 `.mdx` MDict 词典（已代码审查，待 ICC 宿主实测）
- [x] MDict 查词返回词条内容，未加载 MDict 时自动回退到 ECDICT（已代码审查，待 ICC 宿主实测）
- [x] Edge TTS 引擎无需 API Key 即可合成并播放单词音频（已代码审查，待 ICC 宿主实测）
- [x] 配置完成后，Piper TTS 引擎可在本地合成并播放音频（已代码审查，待 ICC 宿主实测）
- [x] 虚拟键盘弹窗可从工具栏按钮打开并接受触摸输入（已代码审查，待 ICC 宿主实测）
- [x] 虚拟键盘包含 QWERTY 布局、退格键、空格键和搜索键
- [x] 结果视图显示单词、音标、发音按钮和标签页分区
- [x] 结果视图能渲染 MDict HTML 和 ECDICT 文本（已代码审查，待 ICC 宿主实测）
- [x] 设置视图包含 MDict 路径、TTS 引擎、Edge 音色语速、Piper 路径模型
- [x] 设置项可持久化保存并在下次启动时恢复
- [x] 插件能成功编译并生成 `.icpx` 包
- [x] README/文档列出需要手动下载的内容（ECDICT 数据、Piper 程序及模型）
